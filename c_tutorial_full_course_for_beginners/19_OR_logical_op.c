#include <stdio.h>



int main(){

    // || (OR) checks if at least one condition are true

    float temp = 25;

    if (temp >= 0 || temp <= 30)
    {
        printf("\nThe weather is good!");
    }
    else
    {
        printf("\nThe weather is bad!");
    }
    


    return 0;
}